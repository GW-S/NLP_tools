1. find. 命令  该命令是用来搜寻文件明的，后面加上 -name "*.txt",就会对文件名进行匹配

2. where is, 该命令是对linux自带的文件，比如bin文件，库文件的文件名进行检测。

3. which, which的实质，是检测路径文件中是否包含这个命令。

4. arxgs 命令,其实质是从命令流里得到命令的命令，有的命令比如 echo,不接受标准输出，而 grep的东西，都是标准输出，所以不能通过管道传递，
只有xargs命令作为一个中转节点，达成最后的效果。

5. Man 可以很方便的查看文档

6. cat 和 tail 都是查看一个文档的内容，在整个过程中，作为一个很有用的东西

7. zip 和 tar 以及 Gzip,其中Gzip只能压缩，而tar只能压缩，两者分别作了不同的事情。

8. chmod 全名叫做，change mode 

9. history 全名叫做历史，在代码上可以呈现出以前用过的信息

10. curl的内容很丰富，和网页的使用密切相关。
